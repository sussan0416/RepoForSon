# CLAUDE.md

## リポジトリの目的

息子(6歳)が遊ぶための実験用リポジトリ。学習・お遊び用のコードを気軽に置く場所。

## 現状

- `index.html`: プロダクト一覧ページ（GitHub Pages のトップ）。
- `turtle-game/`: かめプログラミング（静的 HTML/CSS/JS、ビルド不要）。
- 言語・フレームワークは未定。プロジェクトを追加する際にこのファイルへビルド/実行/テストのコマンドを追記すること。

## プロジェクト: turtle-game（かめプログラミング）

息子が超簡単な言語でステージとかめの動きをプログラムし、10x10のワールドで遊べる Web アプリ。

- 構成: `turtle-game/index.html`, `style.css`, `script.js`（純粋な静的ファイル、ビルドツール無し）
- ローカル実行: `turtle-game/index.html` をブラウザで直接開くか、リポジトリ直下で `python3 -m http.server` を実行して `http://localhost:8000/turtle-game/` にアクセス
- テスト: 自動テストは無し。ブラウザで実行・停止・リセット・ランダムの各ボタンとサンプルプログラムの動作を目視確認する
- デプロイ: GitHub Pages（リポジトリの Settings > Pages で Source を `main` ブランチ / ルートに設定）。ルートの `index.html` から各プロダクトへリンクする構成なので、新しいプロダクトを追加したらここにもリンクを追加すること

## 環境

- ランタイムのバージョン管理は asdf を使用（node, python, ruby など）。新しくプロジェクトを作る場合は `.tool-versions` を用意する。

## 対象読者への配慮

子ども向けの内容になることが多い。説明やコメントは平易に、動かして楽しめるものを優先する。
